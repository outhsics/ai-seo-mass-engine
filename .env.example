# ============================================
# AI-SEO-Mass-Engine 环境变量配置示例
# Environment Variables Configuration Example
# ============================================
# 复制此文件为 .env 并填入您的实际值
# Copy this file to .env and fill in your actual values

# ============================================
# AI APIs / AI 接口配置
# ============================================

# Anthropic Claude API (必需 / Required)
# 用于文章生成 / Used for article generation
# 获取地址: https://console.anthropic.com/
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# OpenAI API (可选 / Optional)
# 用于 AI 图片生成 / Used for AI image generation
# 获取地址: https://platform.openai.com/api-keys
# OPENAI_API_KEY=your_openai_api_key_here

# ============================================
# Deployment / 部署配置
# ============================================

# 部署平台选择 / Deployment platform choice
# 可选值: cloudflare, vercel, netlify
# Options: cloudflare, vercel, netlify
DEPLOY_PLATFORM=cloudflare

# Cloudflare Pages 配置 / Cloudflare Pages Configuration
# 获取地址: https://dash.cloudflare.com/profile/api-tokens
CLOUDFLARE_API_TOKEN=your_cloudflare_api_token
CLOUDFLARE_ACCOUNT_ID=your_cloudflare_account_id

# Vercel 配置 / Vercel Configuration
# 获取地址: https://vercel.com/account/tokens
# VERCEL_TOKEN=your_vercel_api_token
# VERCEL_TEAM_ID=your_team_id
# VERCEL_PROJECT_NAME=your_project_name

# ============================================
# Google Services / Google 服务
# ============================================

# Google Search Console (Sitemap 提交 / Sitemap submission)
# 用于自动提交 sitemap 到 Google / For automatic sitemap submission
# 需要创建服务账号并下载 JSON 密钥文件
# Create service account and download JSON key file
GOOGLE_SERVICE_ACCOUNT_KEY_PATH=./google-credentials.json

# 站点配置 / Site configuration
SITE_URL=https://your-domain.com
SITEMAP_URL=https://your-domain.com/sitemap-index.xml

# Google OAuth (可选 / Optional)
# 用于更高级的 Search Console 功能
# For advanced Search Console features
# GOOGLE_OAUTH_CLIENT_ID=your_client_id
# GOOGLE_OAUTH_CLIENT_SECRET=your_client_secret

# ============================================
# Content Generation / 内容生成配置
# ============================================

# 文章生成设置 / Article generation settings
ARTICLE_MIN_WORDS=1000
ARTICLE_MAX_WORDS=3000
ARTICLES_PER_BATCH=10

# 关键词爬取设置 / Keyword scraping settings
KEYWORD_SOURCE=auto
MAX_KEYWORDS_PER_RUN=100

# ============================================
# Image Generation / 图片生成配置
# ============================================

# DALL-E 配置 / DALL-E Configuration
IMAGE_MODEL=dall-e-3
IMAGE_SIZE=1024x1024
IMAGE_QUALITY=standard
IMAGE_STYLE=vivid

# ============================================
# Analytics & Monitoring / 分析和监控
# ============================================

# 排名检查间隔（小时）/ Ranking check interval (hours)
RANK_CHECK_INTERVAL=24

# A/B 测试配置 / A/B testing configuration
AB_TEST_MIN_SAMPLE_SIZE=100
AB_TEST_CONFIDENCE_LEVEL=0.95

# ============================================
# Data Storage / 数据存储配置
# ============================================

# 数据目录 / Data directories
DATA_DIR=./data
BACKUP_DIR=./backups
KEYWORDS_DIR=./data/keywords
ARTICLES_DIR=./data/articles
REPORTS_DIR=./data/reports

# 备份配置 / Backup configuration
AUTO_BACKUP=true
BACKUP_INTERVAL_HOURS=24
MAX_BACKUPS=10

# ============================================
# Cluster Management / 站群管理配置
# ============================================

# 站群设置 / Site cluster settings
CLUSTER_CONFIG_FILE=./cluster.config.json
MAX_SITES_PER_CLUSTER=50

# 全局仪表板端口 / Global dashboard port
DASHBOARD_PORT=3000

# ============================================
# Internationalization / 国际化配置
# ============================================

# 默认语言 / Default language
# 支持的语言: en-US, zh-CN, ja-JP, ko-KR, es-ES, fr-FR
DEFAULT_LOCALE=en-US

# 支持的语言列表 / Supported locales (comma separated)
SUPPORTED_LOCALES=en-US,zh-CN,ja-JP,ko-KR,es-ES,fr-FR

# ============================================
# Pipeline / 流水线配置
# ============================================

# 配置文件路径 / Configuration file path
CONFIG_PATH=./pipeline.config.json

# 运行环境 / Runtime environment
NODE_ENV=production

# 日志级别 / Log level
# 可选值: debug, info, warn, error
# Options: debug, info, warn, error
LOG_LEVEL=info

# ============================================
# Advanced Settings / 高级设置
# ============================================

# 并发任务数 / Concurrent tasks
MAX_CONCURRENT_TASKS=5

# 请求超时（毫秒）/ Request timeout (ms)
REQUEST_TIMEOUT=30000

# 重试次数 / Retry attempts
MAX_RETRIES=3

# 启用调试模式 / Enable debug mode
# DEBUG=false
